// const paragraph =
//   "OAuth is an authorization framework that allows users to grant limited access to resources without sharing their passwords. ";

// export const hugeDocument = paragraph.repeat(500);


const oauth =
"OAuth is an authorization framework that allows users to grant limited access to resources without sharing passwords. ".repeat(120);

const jwt =
"JWT is a compact token used for authentication between client and server. ".repeat(120);

const react =
"React is a JavaScript library for building user interfaces using reusable components. ".repeat(120);

export const knowledgeBase = {
    oauth,
    jwt,
    react
};