export function logStep(step) {
    console.log(`\n========== ${step} ==========\n`);
}

export function success(message) {
    console.log(`${message}`);
}

export function error(message) {
    console.log(`${message}`);
}